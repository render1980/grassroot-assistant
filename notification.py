#! /usr/bin/python3

import smtplib, ssl

def notify(admins_ids, group, new_user_id):
    # notify
    # TODO: we need chat_id to send message directly to group admin
    return 

def send_notification(admin_id, group, new_user_id):
    return
